# DailyCutReport 0.14.1 release media

Seeded local UI captures for Today, Foods, Cart, Product Editor, Health, Settings, and the expanded widget. No Android device was connected during release preparation, so these captures use the preceding compatible UI seed; refresh them from a physical device when one is available.
