# DailyCutReport app-specific R8 rules.
# AndroidX, Room, CameraX, Health Connect, and bundled ML Kit artifacts provide
# their own consumer rules. Keep this file intentionally minimal so release
# shrinking can remove unused code and resources.
