<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/quick_scan_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_quick_scan_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/quick_scan_widget_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Maintenance"
        android:textColor="@color/dailycut_yellow"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/quick_scan_widget_button"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/widget_quick_scan_button"
        android:gravity="center"
        android:text="Scan"
        android:textColor="@color/dailycut_black"
        android:textSize="18sp"
        android:textStyle="bold" />
</LinearLayout>
